# specify directory as data io info
BASEDIR = 'aces/'
DATADIR = 'data'
MODEL_NAME = 'aces'
MODEL_CHECKPOINT_NAME = 'modelCheckpoint'

# specify some data structure
LABELS = ['class']

# patch size for training
PATCH_SHAPE = (128, 128)

# Sizes of the training and evaluation datasets. 
TRAIN_SIZE = 25438
TEST_SIZE = 8645
VAL_SIZE = 8417

# Specify model training parameters.
BATCH_SIZE = 64
EPOCHS = 10
BUFFER_SIZE = 5000
RAMPUP_EPOCHS = 25
SUSTAIN_EPOCHS = 20

# Rates
USE_ADJUSTED_LR = True
MAX_LR = 1E-3
MID_LR = 3E-4
MIN_LR = 1E-4
LEARNING_RATE = 0.001
DROPOUT_RATE = 0.2

CALLBACK_PARAMETER = 'val_f1_m'
# ACTIVATION_FN autogenerated in the main script
ACTIVATION_FN = 'softmax'
OPTIMIZER = 'adam' # 'custom', 'adam'
# 'custom_focal_tversky_loss'
LOSS = 'categorical_crossentropy'
# choices are: cnn, dnn, unet, 
MODEL_TYPE = 'unet'
METRICS = 'precision
recall
f1_m,
dice_coef,
one_hot_io_u'

OUT_CLASS_NUM = 5

# EE settings
EE_USER = 'aces'
EE_OUPUT_ASSET = 'aces'

EE_SERVICE_CREDENTIALS = 'your-service-credentials.json'

# GCS settings
GCS_PROJECT = 'mygcs-aces'
GCS_BUCKET = 'mygcs-aces'
GCS_EEIFIED_DIR = 'eeified'
GCS_CHECKPOINT_DIR = 'checkpoints'

FEATURES = 'red_before
green_before
blue_before
nir_before
red_during
green_during
blue_during
nir_during'
